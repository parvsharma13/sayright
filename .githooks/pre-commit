#!/bin/sh
set -eu
if ! command -v gitleaks >/dev/null 2>&1; then
    echo 'Install Gitleaks before committing: brew install gitleaks' >&2
    exit 1
fi
exec gitleaks git --pre-commit --staged --redact=100 --no-banner .
